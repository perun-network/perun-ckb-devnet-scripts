<h1 align="center"><br>
    <a href="https://perun.network/"><img src=".assets/go-perun.png" alt="Perun" width="196"></a>
<br></h1>

<h2 align="center">Perun-CKB-Devnet-Scripts</h2>

<p align="center">
  <a href="https://www.apache.org/licenses/LICENSE-2.0.txt"><img src="https://img.shields.io/badge/license-Apache%202-blue" alt="License: Apache 2.0"></a>
</p>

This repository contains scripts to setup a local devnet (nervos). This branch contains scripts with vc contracts.

## Dependencies
The  `setup-devnet.sh` requires the following tools:
* `jq`:
  - Used to parse and edit some configuration files.
* `sed` and `awk`:
  - We modify some fields of the files generated by the `ckb init --chain dev` command using `sed` and `awk`.
* `tmux` and `tmuxp`:
  - `tmuxp` is a session manager for `tmux` that allows to easily create descriptive `.yaml` configuration files to create and attach to `tmux` sessions.
* `expect`:
  - We completely automize the process for test-wallet creation, deploying of contracts etc. To make this work reliably, we use `expect` which allows to describe how a commandline application is fed input.
* `make`:
  - Not strictly necessary, but it should be available on most systems by default. In case you do not want to install `make` check out the `Makefile` content and issue the command on your own.
* `ckb` with version `0.201.0` or higher.
* `ckb-cli` with version `1.13.0` or higher.

## Start Devnet
To start devnet execute `make dev`in the project root.

## Config
By default the contracts don't use type-id. If you want to use them, simply edit the `deployment.toml` and `deplyoment_vc.toml` files.

## Potential Problems
### 1. Python Interpreter
On some Linux distributions, the Python interpreter is accessible as python3 instead of python.The fund_accounts.sh script expects python to be available.

### 2. Ckb rpc
Ensure that the ckb-cli's rpc is set to `http://127.0.0.1:8114`. 